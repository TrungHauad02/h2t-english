export interface User {
    id: String,
    avatar: String,
    content_motivation: String,
    email: String,
    end_date: Date,
    level_enum: String,
    name: String,
    password: String,
    role_enum: String,
    start_date: Date,
    status: boolean
}
